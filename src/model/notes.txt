 // branch_gst: Set(input.branch_gst.clone()),
            // party_gst: Set(input.party_gst.clone()),
            // gst_location_type: Set(gst_location_type),
            // lut: Set(input.lut.unwrap_or_default()),
            // rcm: Set(input.rcm.unwrap_or_default()),
            // cancelled: Set(false),
            // memo: Set(input.memo),
            // party: Set(input.party),